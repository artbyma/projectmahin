// Hardcode the token ids that are defined.
const TokenIds = [
  {
    id: 1,
  },
];

// For each we want to know the state, which is:
// 1. price
// 2. current image/state
// 3. owner

function Gallery() {}
