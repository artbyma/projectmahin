type State @entity {
  id: ID!
  currentRoll: Roll
  rollCount: Int!
  diagnosedCount: Int!
}

type Roll @entity {
  id: ID!
  requestedAt: BigInt!
  requestTxHash: String!
  appliedAt: BigInt
  applyTxHash: String
}

type Diagnosis @entity {
    id: ID!
    roll: Roll!
    tokenId: Int!
}
